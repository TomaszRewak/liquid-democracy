{
    "version": "2.0.0",
    "tasks": [
        {
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "cwd": "${workspaceRoot}/backend"
            },
            "group": "build",
            "label": "build-backend",
            "presentation": {
                "clear": true
            }
        }
    ]
}